const screen = document.getElementById("screen");
const context = screen.getContext("2d");

// // drawing the white background
// context.fillStyle = "white";
// context.fillRect(0, 0, screen.width, screen.height);

// context.fillStyle = "black";
// context.font = "20px Arial";

// // various points marked on the screen
// context.fillRect(0, 0, 5, 5);
// context.fillText("(0, 0)", 10, 20);

// context.fillRect(300, 20, 5, 5);
// context.fillText("(300, 20)", 310, 30);

// context.fillRect(100, 100, 5, 5);
// context.fillText("(100, 100)", 110, 110);

// context.fillRect(130, 350, 5, 5);
// context.fillText("(130, 350)", 140, 360);

// context.fillRect(screen.width / 2, screen.height / 2, 5, 5);
// context.fillText(`(${screen.width / 2}, ${screen.height / 2})`, screen.width / 2 + 10, screen.height / 2 + 10);

// context.fillRect(screen.width -5, screen.height -5, 5, 5);
// context.fillText(`(${screen.width}, ${screen.height})`, screen.width - 100, screen.height - 10);
